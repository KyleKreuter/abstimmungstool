spring.application.name=abstimmungstool

server.port=4222
spring.config.import=optional:file:.env[.properties]

# MySQL connection
spring.datasource.url=jdbc:mysql://abstimmungstool-db:3306/abstimmungstool
spring.datasource.username=abstimmung
spring.datasource.password=abstimmung

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Admin credentials
app.admin.username=${ADMIN_USERNAME}
app.admin.password=${ADMIN_PASSWORD}
